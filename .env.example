# Supabase — these are client-side keys, safe to expose in the browser bundle.
# The security boundary is Row Level Security, not key secrecy.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-or-anon-key
